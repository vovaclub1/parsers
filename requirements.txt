# FIX: пины версий — без них при пересборке образа могут прилететь breaking changes.
# Минимальные верхние границы блокируют только следующий major.

# HTTP + Telegram
requests>=2.31,<3
urllib3>=1.26,<3
aiohttp>=3.9,<4
websockets>=12,<13
python-dotenv>=1.0,<2

# FIX-batch-1: speedups (drop-in)
# uvloop      — asyncio event loop в 2-4x быстрее (Cython + libuv)
# orjson      — JSON парсинг в 3-5x быстрее std json (Rust)
# tgcrypto    — C-расширение AES для Telethon/Pyrogram, x10 быстрее decode
# msgspec     — schema-based JSON: orjson + dict→struct в один проход
# httpx[http2]— HTTP/2 клиент с мультиплексированием (для background TP/SL)
uvloop>=0.19,<1
orjson>=3.9,<4
msgspec>=0.18,<1
httpx[http2]>=0.27,<1

# curl_cffi — requests-совместимый HTTP-клиент с импersonation
# реальных TLS-fingerprint'ов (Chrome/Safari/Firefox). Без него
# Binance CDN (Akamai) детектит python-requests JA3 и возвращает
# 400 на /bapi/composite/v1/public/cms/article/list/query.
curl_cffi>=0.7,<1

# Telegram clients
telethon>=1.36,<2
cryptg>=0.4,<1
pyTelegramBotAPI>=4.14,<5

# Crypto
ccxt>=4.2
solders>=0.20
base58>=2.1,<3
hyperliquid-python-sdk>=0.7
eth-account>=0.10
